/** @type {import('next').NextConfig} */
const nextConfig = {
  // NẾU XUẤT HIỆN LỖI THÌ CHO CÁI NÀY THÀNH TRUE
  // CÁI NÀY THÀNH FALSE ĐỂ USE EFFECT CHỈ CHẠY 1 LẦN
  reactStrictMode: true,
};

module.exports = nextConfig;
